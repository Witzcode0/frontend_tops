/*

In JavaScript, a string is a sequence of characters enclosed within quotation marks (either single ' or double "). It's a fundamental data type used to represent text, words, sentences, or any other textual information.

Creating Strings:

Single Quotes:

const greeting = 'Hello, world!';

Double Quotes:

const message = "This is a string with double quotes.";

Template Literals (ES6):
Use backticks (`) to create multi-line strings and interpolate expressions.
Example:

const name = 'Alice';
const age = 30;
const templateString = `My name is ${name} and I am ${age} years old.`;

Accessing Characters:

Use the index of a character to access it (starting from 0).
Example:

const word = 'JavaScript';
console.log(word[0]); // Output: "J"
*/

const name = "tops technologies pvt. ltd.";

// console.log(name.length)
// console.log(name.charAt(1))
// console.log(name.toUpperCase().toLowerCase().replace(" ", "-").replace(" ", "-").replace(" ", "-"))
// console.log(name.indexOf("n"))
// console.log(name.lastIndexOf("n"))
// console.log(name.lastIndexOf())



