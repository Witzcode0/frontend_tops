// Javascript Hoisting

// Hoisting is javascript's default behavior of moving declarations to the top.

// Javascript Declaration are hoisted

// - In JS, a variable can be declared after it has been used.

// In  other words, a variable can be used befor it has beed declared

// Hoisting is JavaScript's default behavior of moving declarations to the top of the current scope (to the top of the current script or the current function) 

x = 10;

console.log(x);

var x;
