// JavaScript Strict Mode
// Strict mode in JavaScript is a way to opt into a restricted variant of JavaScript, catching errors earlier and preventing certain problematic behaviors. It is activated by adding "use strict"; at the top of the script or function.

"use strict";

function myFunction() {
  x = 3.14; // Throws an error because x is not declared
}

myFunction();
